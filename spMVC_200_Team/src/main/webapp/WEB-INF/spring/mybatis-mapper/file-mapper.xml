<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team.starbucks.dao.ext.FileDao">

	<select id="selectAll">
		SELECT * FROM tbl_files
	</select>

	<select id="findById">
		SELECT * FROM tbl_files
		WHERE file_seq = #{file_seq}
	</select>

	<insert id="insert">
		INSERT INTO tbl_files
		(file_originalName, file_upname)
		VALUES
		(#{file_originalName}, #{file_upname} )
	</insert>

	<insert id="insertOrUpdateWithList" parameterType="FileDTO">
		INSERT INTO tbl_files
		(file_seq, file_originalName, file_upname)
		VALUES
		<foreach collection="filesList" item="file" separator=", ">
			(
			#{file.file_seq},
			#{file.file_originalName},
			#{file.file_upname}
			)
		</foreach>
		AS NEW(file_seq, file_originalName, file_upname)
		ON DUPLICATE KEY UPDATE
		file_originalName 	= #{file_originalName},
		file_upname 		= #{file_upname}
	</insert>

	<update id="update">

	</update>

	<insert id="insertOrUpdate" parameterType="FileDTO">
 		INSERT INTO tbl_files (
 			file_seq, file_originalName, file_upname
 		) VALUES (
 			#{file_seq}, #{file_originalName},	#{file_upname}
 		)
 		ON DUPLICATE KEY UPDATE
	 		file_originalName 	= #{file_originalName},
	 		file_upname 		= #{file_upname}
	</insert>

	<delete id="delete">
		DELETE FROM tbl_files
		WHERE file_seq = #{file_seq}
	</delete>

	<update id="create_table">
		CREATE TABLE IF NOT EXISTS tbl_files(
		file_seq BIGINT 	AUTO_INCREMENT PRIMARY KEY,
		file_originalName 	VARCHAR(256) NOT NULL,
		file_upname 		VARCHAR(256) NOT NULL
		)
	</update>

</mapper> 